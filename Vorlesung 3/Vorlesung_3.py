# Methode als Hauptdatei setzen
"""
if __name__ == "__main__":
    main()
"""
# Platzsparend programmieren
"""
print(round(float(input("Gib mir eine Kommazahl: ").strip().replace(",", ".")) * float(input("Gib mir noch eine Kommazahl: ").strip().replace(",", ".")), 1))
"""

# Tuples und Dictionaries auf den Folien gut erklärt